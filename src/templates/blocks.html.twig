{% block sortable %}
    {% if sortable is defined %}
        <script>
            var IS_SORTABLE_ENABLED = true;
            var ENTITY_NAMESPACE = "{{ sortable|escape('js') }}";
        </script>
    {% endif %}
{% endblock %}
